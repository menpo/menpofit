package:
  name: menpofit
  version: {{ environ['CONDACI_VERSION'] }}

requirements:
  build:
    - python
    - setuptools
    - cython 0.23.*

  run:
    - python
    - menpo 0.6.*
    - scikit-learn 0.16.*
    - dlib 18.16

test:
  requires:
    - nose
    - mock 1.0.1

  imports:
    - menpofit

about:
  home: https://github.com/menpo/menpofit/
  license: BSD
